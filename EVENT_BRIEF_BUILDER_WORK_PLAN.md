# EVENT BRIEF BUILDER
## AI 친화형 이벤트 기획서 생성기 작업기획서 v0.1

- 문서 목적: Claude Code로 바로 개발을 시작할 수 있는 MVP 기준 작업기획서
- 프로젝트명(가칭): `event-brief-builder`
- 배포 방식: Vercel 정적 웹앱
- 최종 목표: 추후 GODO AI OS 내부의 기획서 작성 모듈 및 이미지 생성 모듈로 이식

---

## 1. 프로젝트 배경

현재 상품팀·마케팅팀·CS팀은 이벤트 페이지, 무료체험단, 할인전, 론칭, 팝업 등의 이미지 제작을 엑셀 기획서로 요청한다.

기존 엑셀 기획서는 다음 문제가 있다.

- 완성 디자인 시안, 참고 이미지, 실제 작업 지시, 퍼블리싱 링크가 한 화면에 뒤섞인다.
- 빨간 박스, 화살표, 검정 박스, 셀 메모 등 작성자마다 지시 방식이 다르다.
- 디자인 작업자는 문맥을 읽어야 하고, AI는 지시문과 참고 이미지를 혼동할 수 있다.
- 이미지 생성 AI가 기획서의 색상·도형 모양을 규칙으로 오인하면 하드코딩성 예외 처리가 누적된다.
- 실제로 중요한 것은 도형 모양이 아니라 문구·이미지·강조·관계·대략적 위치다.

따라서 기존 엑셀을 더 잘 읽는 파서를 만드는 대신, 처음부터 사람도 만들기 쉽고 AI도 오해하지 않는 구조화 기획서 생성기를 만든다.

---

## 2. 핵심 정의

이 프로그램은 디자인 툴이 아니다.

> 비디자이너가 블록을 쌓듯 문구, 이미지, 제품, 혜택, 버튼 등을 대략 배치하고 의미를 지정하면, AI가 바로 읽을 수 있는 구조화 기획서 파일을 생성하는 도구다.

최종 디자인은 별도의 이미지 생성기가 담당한다.

```text
[기획서 생성기]
  사람의 요구를 구조화
        ↓
[.eventbrief 파일]
  JSON + 이미지 + 미리보기
        ↓
[이미지 생성기]
  자유로운 최종 디자인 생성
```

---

## 3. 최우선 설계 원칙

### 3.1 의미 중심

블록은 최종 디자인 컴포넌트가 아니라 의미 역할이다.

예:

- `메인 문구` 블록 = 가장 중요한 헤드라인
- `제품 이미지` 블록 = 반드시 사용할 제품 자산
- `버튼` 블록 = CTA 문구와 목적
- `참고 링크` 블록 = 이미지 생성 AI가 사용하지 않는 퍼블리싱 정보

블록의 모양·색상·좌표를 그대로 최종 디자인에 강제하지 않는다.

### 3.2 위치는 소프트 힌트

기획자가 배치한 위치와 크기는 참고값이다.

- 상단/중단/하단
- 좌/중앙/우
- 크게/보통/작게
- 먼저/나중
- 한 묶음

이미지 생성기는 전체 품질을 위해 위치와 크기를 자유롭게 재구성할 수 있다.

### 3.3 필수 내용과 참고 내용을 분리

반드시 구분한다.

- 디자인에 실제로 들어갈 정보
- AI가 참고할 정보
- 퍼블리싱 단계에서만 사용할 정보

### 3.4 하드코딩 금지

다음과 같은 판별 규칙을 만들지 않는다.

- 빨간 박스면 지시문
- 검정 박스면 일반 문구
- 화살표가 없으면 관계 없음
- 특정 레이아웃이면 특정 행사 유형

기획서 생성 단계에서 사용자가 직접 블록의 의미를 선택하므로 색상·도형 추론이 필요 없다.

### 3.5 자유로운 최종 레이아웃 보장

기획서 생성기가 특정 디자인 템플릿을 강제하지 않는다.

- 제품 1개와 12개 모두 지원
- 할인전, 무료체험단, 론칭, 팝업, 구매 사은품 등 모두 지원
- 섹션 수와 캔버스 높이 제한 없음

---

## 4. 대상 사용자

- 상품팀
- 마케팅팀
- CS팀
- 기타 디자인 요청 부서

사용자 전제:

- 디자인 전문 지식 없음
- 엑셀보다 쉬운 조작 필요
- 마우스 드래그, 클릭, 텍스트 입력, 복사·붙여넣기 중심
- 별도 교육 없이 5분 내 첫 기획서 작성 가능해야 함

---

## 5. MVP 범위

### 포함

- 새 기획서 생성
- 블록 드래그 앤 드롭
- 블록 이동·크기 조절
- 블록 내용 편집
- 이미지 파일 업로드
- 클립보드 이미지 붙여넣기
- 캔버스 높이 조절
- 디자인 정보와 퍼블리싱 정보 분리
- 로컬 자동저장
- 기획서 미리보기
- `.eventbrief` 내보내기
- `.eventbrief` 다시 불러오기
- PNG 미리보기 내보내기
- 필수값 검증
- Vercel 배포

### 제외

- AI 이미지 생성
- OpenAI/Claude API 연결
- 로그인
- 서버 DB
- 다중 사용자 동시 편집
- 승인 워크플로우
- GODO AI OS 직접 통합
- 실제 HTML 퍼블리싱
- 상품 DB 자동연결

MVP에서는 기획서 작성 경험과 파일 구조를 먼저 검증한다.

---

## 6. 화면 구조

권장 데스크톱 3열 구조:

```text
┌──────────────┬──────────────────────────┬──────────────────┐
│ 블록 팔레트   │ 840px 기준 기획 캔버스    │ 선택 블록 설정     │
│              │                          │                  │
│ 문구          │ 드래그·이동·크기 조절      │ 내용              │
│ 이미지        │ 대략적인 위치 표현         │ 중요도            │
│ 행사정보      │                          │ 필수 여부          │
│ 참고/링크     │                          │ AI 전달 여부       │
└──────────────┴──────────────────────────┴──────────────────┘
```

상단 툴바:

- 새로 만들기
- 불러오기
- 저장 상태
- 실행 취소/다시 실행
- 미리보기
- 기획서 내보내기

---

## 7. 블록 체계

블록은 두 그룹으로 명확히 분리한다.

### 7.1 디자인 입력 블록

이미지 생성 AI가 읽는 정보다.

#### 텍스트

- 메인 문구
- 서브 문구
- 본문 문구
- 강조 문구
- 기간
- 가격
- 할인율
- 주의 문구
- 자유 텍스트

#### 이미지

- 로고
- 메인 제품 이미지
- 보조 제품 이미지
- 제품 그룹
- 배경 참고 이미지
- 기존 통이미지 그대로 삽입
- GIF/움짤 원본

#### 행사 구조

- 혜택
- 구매 조건
- 사은품
- 신청 조건
- 선택 항목
- 당첨 인원
- 제품명
- CTA 버튼
- 구분선/섹션 제목
- 그룹 컨테이너

### 7.2 참고·퍼블리싱 블록

이미지 생성 AI가 기본적으로 읽지 않는 정보다.

- 상품 상세페이지 링크
- 버튼 연결 URL
- 신청 폼 링크
- 참고 사이트 링크
- GIF 원본 링크
- 퍼블리싱 메모
- 담당자 메모
- 기존 페이지 위치
- 수정 요청 참고자료

UI에서 디자인 블록과 다른 색상/영역으로 명확히 구분한다.

---

## 8. 블록 공통 속성

모든 블록은 다음 공통 속성을 가진다.

```ts
interface BriefBlock {
  id: string;
  type: BlockType;
  label: string;
  content?: string;
  required: boolean;
  priority: 1 | 2 | 3 | 4 | 5;
  aiVisibility: 'design' | 'reference' | 'publishing';
  position: {
    x: number;
    y: number;
    width: number;
    height: number;
  };
  layoutHint: {
    region?: 'top' | 'middle' | 'bottom' | 'free';
    alignment?: 'left' | 'center' | 'right' | 'free';
    emphasis?: 'low' | 'normal' | 'high' | 'very_high';
    order?: number;
  };
  notes?: string;
  assetId?: string;
  groupId?: string;
}
```

좌표는 840px 기준 캔버스 좌표로 저장하되 AI에는 소프트 힌트로 전달한다.

---

## 9. 블록별 편집 예시

### 메인 문구

```text
내용: 롬프 1+1 EVENT
필수: 예
중요도: 매우 높음
위치 힌트: 상단 중앙
추가 지시: 크게 강조
```

### 제품 이미지

```text
제품명: 롬프 스위치 X
이미지: 붙여넣기/업로드
역할: 메인 제품
필수: 예
중요도: 높음
```

### 기존 통이미지

```text
처리: 수정하지 않고 그대로 삽입
위치 힌트: 하단
AI 변형 허용: 아니오
```

### 퍼블리싱 링크

```text
용도: CTA 버튼 연결
URL: https://...
이미지 생성 AI 전달: 아니오
```

---

## 10. 캔버스 동작

- 기본 폭: 840px
- 기본 높이: 1800px
- 높이 직접 입력 또는 하단 핸들로 연장
- 확대/축소 지원
- 블록 자유 이동
- 블록 크기 조절
- 그리드 스냅 ON/OFF
- 정렬 가이드
- 복제
- 삭제
- 잠금
- 그룹화/그룹 해제
- 앞/뒤 순서 변경

중요:

캔버스는 최종 디자인 화면이 아니라 기획 의도 배치판이다.
정교한 폰트·색상·효과 편집 기능은 넣지 않는다.

---

## 11. 이미지 입력 UX

필수 지원:

- 파일 선택
- 드래그 앤 드롭
- Windows 스크린샷 후 `Ctrl+V`
- 여러 이미지 동시 업로드
- PNG/JPG/WEBP/GIF
- 투명 배경 유지
- 원본 파일명 보존

이미지 등록 시 다음을 입력할 수 있다.

- 제품명
- 이미지 역할
- 변형 허용 여부
- 필수 사용 여부
- 동일 제품군 그룹

---

## 12. 프로젝트 기본 정보

기획서 상단에서 입력:

- 프로젝트명
- 요청 부서
- 작성자
- 작성일
- 행사 유형
- 작업물 유형
  - 이벤트 페이지
  - 팝업
  - 배너
  - SNS 이미지
  - 기타
- 목표 출력 폭
- 예상 세로 길이
- 자유 콘셉트 메모

행사 유형은 메타데이터일 뿐 레이아웃 템플릿을 강제하지 않는다.

---

## 13. `.eventbrief` 파일 구조

`.eventbrief`는 내부적으로 ZIP 형식이다.

```text
project-name.eventbrief
├─ manifest.json
├─ brief.json
├─ preview.png
├─ assets/
│  ├─ logo.png
│  ├─ product-01.png
│  └─ product-02.gif
└─ references/
   ├─ existing-page.jpg
   └─ notes.json
```

### manifest.json

```json
{
  "format": "eventbrief",
  "version": "1.0.0",
  "createdAt": "2026-07-23T00:00:00Z",
  "generator": "event-brief-builder"
}
```

### brief.json 핵심 구조

```json
{
  "schemaVersion": "1.0.0",
  "project": {
    "title": "8월 무료체험단",
    "requestTeam": "마케팅팀",
    "outputType": "event_page",
    "canvasWidth": 840,
    "canvasHeight": 2400
  },
  "blocks": [],
  "assets": [],
  "designSummary": {
    "requiredTexts": [],
    "requiredProducts": [],
    "requiredBenefits": [],
    "layoutHints": []
  },
  "publishing": {
    "links": [],
    "notes": []
  }
}
```

`brief.json`이 유일한 원본 데이터다.
`preview.png`는 사람이 빠르게 확인하기 위한 보조자료다.

---

## 14. 내보내기 전 검증

내보내기 버튼 클릭 시 자동 검사한다.

### 오류

- 프로젝트명 없음
- 디자인 블록 0개
- 필수 블록 내용 없음
- 이미지 블록에 이미지 없음
- 동일 블록 ID 충돌
- 참조 파일 누락

### 경고

- 메인 문구 없음
- 필수 제품이 1개도 없음
- 버튼 문구는 있으나 연결 정보 없음
- 링크가 디자인 영역에 배치됨
- 기존 통이미지에 변형 허용이 켜져 있음
- 캔버스 밖 블록 존재

오류는 내보내기 차단, 경고는 사용자 확인 후 허용한다.

---

## 15. 자동 요약 데이터

기획자가 별도로 AI 프롬프트를 쓰지 않아도 기획서 생성기가 다음 요약을 자동 생성한다.

```text
필수 메인 문구
필수 서브 문구
필수 제품 목록
행사 혜택과 조건
버튼 문구
기간
주의문구
그대로 삽입할 이미지
무시할 퍼블리싱 링크
대략적인 배치 우선순위
```

이 요약은 규칙 기반으로 생성하며 MVP에서는 AI 호출을 사용하지 않는다.

---

## 16. 로컬 저장

MVP는 서버 없이 동작한다.

- 현재 작업: IndexedDB 자동저장
- 텍스트 설정: localStorage 가능
- 이미지 바이너리: IndexedDB 저장
- 3초 이내 디바운스 자동저장
- 브라우저 재실행 후 복구
- 새 프로젝트 생성 시 기존 작업 저장 확인

---

## 17. 기술 스택 권장

- Vite
- React
- TypeScript strict
- CSS Modules 또는 기존 GODO 이전을 고려한 일반 CSS 구조
- 상태관리: Zustand
- 드래그 앤 드롭: dnd-kit
- 이동·리사이즈: react-rnd 또는 interact.js 중 검증 후 선택
- ZIP 생성/해제: JSZip
- 로컬 DB: Dexie(IndexedDB)
- 미리보기 PNG: html-to-image
- 테스트: Vitest + Playwright
- 배포: Vercel

### Vite를 우선하는 이유

- MVP는 백엔드가 필요 없는 정적 웹앱
- 기존 GODO AI OS의 React/Vite 컴포넌트 구조로 이식하기 쉬움
- Next.js 서버 구조를 먼저 도입할 필요 없음

---

## 18. 권장 폴더 구조

```text
src/
├─ app/
│  ├─ App.tsx
│  └─ routes.tsx
├─ components/
│  ├─ palette/
│  ├─ canvas/
│  ├─ inspector/
│  ├─ toolbar/
│  └─ dialogs/
├─ domain/
│  ├─ briefSchema.ts
│  ├─ blockTypes.ts
│  ├─ validation.ts
│  └─ summaryBuilder.ts
├─ features/
│  ├─ project/
│  ├─ blocks/
│  ├─ assets/
│  ├─ export/
│  └─ import/
├─ stores/
│  └─ briefStore.ts
├─ services/
│  ├─ indexedDb.ts
│  ├─ eventBriefExporter.ts
│  ├─ eventBriefImporter.ts
│  └─ previewRenderer.ts
├─ styles/
└─ types/
```

도메인 로직과 UI 로직을 분리해 추후 GODO AI OS로 옮기기 쉽게 한다.

---

## 19. 개발 단계

### Phase 0. 저장소 생성

- 신규 폴더 및 신규 Git 저장소
- 기존 GODO AI OS 저장소 절대 수정 금지
- Vite + React + TypeScript 생성
- lint, typecheck, build 설정

완료 조건:

- `npm run lint`
- `npm run typecheck`
- `npm run build`
- 모두 통과

### Phase 1. 도메인 스키마

- 프로젝트 타입
- 블록 타입
- 자산 타입
- 검증 규칙
- 샘플 JSON

완료 조건:

- UI 없이 샘플 기획서 JSON 생성 가능
- import/export 단위 테스트 통과

### Phase 2. 기본 UI

- 상단 툴바
- 왼쪽 팔레트
- 중앙 캔버스
- 오른쪽 속성 패널

완료 조건:

- 블록 생성·선택·삭제 가능

### Phase 3. 캔버스 편집

- 드래그
- 이동
- 리사이즈
- 복제
- 그룹화
- 실행 취소/다시 실행

완료 조건:

- 20개 이상 블록에서도 정상 동작

### Phase 4. 이미지 자산

- 업로드
- 드래그 앤 드롭
- Ctrl+V
- 다중 업로드
- 투명 PNG/GIF 보존

완료 조건:

- 브라우저 새로고침 후 이미지 복구

### Phase 5. 분리된 정보 영역

- 디자인 입력
- 참고자료
- 퍼블리싱 정보

완료 조건:

- AI용 요약에 퍼블리싱 링크가 포함되지 않음

### Phase 6. 내보내기·불러오기

- `.eventbrief` 생성
- ZIP 해제
- preview.png 생성
- 동일 상태 복원

완료 조건:

- 내보낸 파일을 다시 열었을 때 블록·이미지·좌표가 동일

### Phase 7. 검증·샘플

기존 사례 기반 샘플 4종을 fixture로 작성한다.

- 텐가 할인 이벤트
- 우머나이저 무료체험단
- 롬프 구매 사은품 이벤트
- 기존 페이지 일부 수정 요청

완료 조건:

- 서로 다른 구조를 템플릿 없이 작성 가능

### Phase 8. 배포 및 문서

- Vercel 배포
- 사용자 1페이지 가이드
- 개발자 문서
- MASTER_REPORT 작성

---

## 20. 핵심 사용자 시나리오

### 시나리오 A. 할인 이벤트

1. 새 프로젝트 생성
2. 메인 문구 추가
3. 할인율 블록 추가
4. 제품 이미지 6개 붙여넣기
5. 제품 그룹으로 묶기
6. 기간과 CTA 추가
7. 대략 배치
8. `.eventbrief` 내보내기

### 시나리오 B. 무료체험단

1. 행사명과 기간 추가
2. 남성용/여성용 선택 그룹 생성
3. 각 제품 이미지와 당첨 인원 입력
4. 신청 조건 추가
5. 상품·폼 링크는 퍼블리싱 탭에 입력
6. 내보내기

### 시나리오 C. 기존 이미지 일부 재사용

1. 새 디자인 영역 블록 생성
2. 기존 통이미지 업로드
3. `수정하지 않고 그대로 삽입` 선택
4. 퍼블리싱 링크 별도 입력
5. 내보내기

---

## 21. 완료 기준

MVP는 다음을 만족하면 완료다.

- 비디자이너가 5분 안에 기획서를 만들 수 있다.
- 이미지 붙여넣기와 드래그가 자연스럽다.
- 색상·박스·화살표 규칙 없이 의미가 구조화된다.
- 디자인 정보와 퍼블리싱 정보가 완전히 분리된다.
- 제품 수와 행사 구조가 달라도 자유롭게 배치할 수 있다.
- 내보낸 파일을 다시 열면 상태가 완전히 복구된다.
- 이미지 생성 AI가 `brief.json`만 읽어도 필수 요구사항을 파악할 수 있다.
- 기존 GODO AI OS 코드에 영향이 없다.

---

## 22. 금지사항

- 행사별 고정 템플릿 추가 금지
- 특정 기획서 사례 전용 분기 금지
- 빨간색·화살표 등 시각 특징 기반 판별 금지
- 이미지 생성 기능 선행 구현 금지
- 백엔드·로그인 선행 구현 금지
- GODO AI OS 저장소 직접 작업 금지
- 검증 없이 커밋·머지·배포 금지

---

## 23. Git 및 검증 원칙

권장 브랜치:

```text
main
feature/brief-schema-v0
feature/brief-canvas-v0
feature/brief-assets-v0
feature/eventbrief-export-v0
```

각 단계마다:

1. 구현
2. lint
3. TypeScript 검사
4. 단위 테스트
5. Playwright 핵심 동작 확인
6. 빌드
7. 문서 갱신
8. 커밋

기능이 검증된 뒤에만 main에 `--no-ff` 머지한다.

---

## 24. Claude Code 첫 실행 지시문

```text
새 프로젝트 `event-brief-builder`를 별도 폴더와 별도 Git 저장소로 시작한다.
기존 GODO AI OS 저장소와 파일은 절대 수정하지 않는다.

첨부된 EVENT_BRIEF_BUILDER_WORK_PLAN.md를 프로젝트의 단일 기준 문서로 사용한다.
처음부터 전체 기능을 만들지 말고 Phase 0과 Phase 1만 진행한다.

Phase 0:
- Vite + React + TypeScript strict
- lint/typecheck/build 스크립트
- 기본 폴더 구조

Phase 1:
- Project, BriefBlock, Asset, PublishingInfo 타입
- blockTypes 상수
- brief validation
- AI용 designSummary 생성기
- 샘플 fixture 1개
- Vitest 단위 테스트

완료 후 변경 파일, 설계 결정, 검증 결과를 보고하고 멈춘다.
자동으로 다음 단계로 넘어가지 않는다.
```

---

## 25. 다음 프로젝트와의 연결

기획서 생성기 검증 후 별도 이미지 생성기를 개발한다.

이미지 생성기는 `.eventbrief`를 입력받아:

- 필수 문구
- 필수 제품
- 혜택 관계
- 소프트 배치 힌트
- 그대로 삽입할 이미지
- 무시할 퍼블리싱 정보

를 읽고 자유롭게 디자인한다.

두 모듈은 최종적으로 GODO AI OS 디자인팀 워크스페이스에 이식한다.
